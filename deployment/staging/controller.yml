apiVersion: v1
kind: Service
metadata:
  name: qbo-api
  namespace: integrations
  labels:
    app: qbo-api
spec:
  ports:
    - port: 8000
  selector:
    app: qbo-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbo-api
  namespace: integrations
  labels:
    app: qbo-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qbo-api
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: qbo-api
    spec:
      containers:
        - name: qbo-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_qbo-api:{{RELEASE_VERSION}}
          resources:
            limits:
              memory: 450Mi
            requests:
              memory: 200Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /api/workspaces/ready/
              port: 8000
            initialDelaySeconds: 10
            timeoutSeconds: 1
          envFrom:
            - secretRef:
                name: qbo-api-secrets
      imagePullSecrets:
        - name: myregistrykey2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbo-api-qcluster
  namespace: integrations
  labels:
    app: qbo-api-qcluster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: qbo-api-qcluster
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: qbo-api-qcluster
    spec:
      containers:
        - name: qbo-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_qbo-api:{{RELEASE_VERSION}}
          command: [ '/bin/bash', 'start_qcluster.sh' ]
          resources:
            limits:
              memory: 600Mi
            requests:
              memory: 600Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          envFrom:
            - secretRef:
                name: qbo-api-secrets
      imagePullSecrets:
        - name: myregistrykey2